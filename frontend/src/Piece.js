class Piece
{
    constructor(rowInd, colInd)
    {
        this.rowInd = rowInd;
        this.colInd = colInd;
    }

    // Assigns a section of the image to a piece
    // as well as draw it on the canvas
    draw(context)
    {
        
    }
}

export default Piece